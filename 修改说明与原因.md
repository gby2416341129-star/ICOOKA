# 酷咔管理系统 1.3.0：完整源码与 UI 修改说明

## 1. 本源码包是什么

这是当前已交付 `KukaManager-1.3.0-Windows-x64.zip` 对应的完整源码快照，包含：

- `src/KukaManager`：主程序完整 C# / WPF 源码。
- `tests/KukaManager.SmokeTests`：Windows WPF、主题控件、数据库和核心流程冒烟测试。
- `tests/KukaManager.DataSmokeTests`：可在非 Windows 构建环境运行的数据层冒烟测试。
- `packaging`：启动脚本、卸载脚本、使用说明和版本说明。
- 根目录构建脚本：Windows 下构建、运行和诊断脚本。

源码包刻意不包含 `bin`、`obj`、`dist` 等可重新生成的编译产物，避免旧 DLL 混入后再次出现“源码已改但运行的还是旧版本”的情况。

## 2. 本轮修改的设计原则

本轮不是针对截图逐页打补丁，而是把“对不齐、拥挤、默认 WPF 控件老旧”视为全局设计系统问题统一处理：

1. 页面内容遵循统一水平基线。
2. 卡片、工具栏、表单和表格使用统一间距体系。
3. 输入控件和按钮不再依赖默认 WPF 外观。
4. DataGrid 表头和正文使用相同左右内边距。
5. 日期选择器完全替换默认老式日历视觉。
6. 保持 SQLite 数据结构、业务规则和已有数据兼容性不变。

## 3. 主要源码改动与原因

### `src/KukaManager/Design/Theme.cs`

改动：

- 建立全局颜色、字体、控件高度、圆角、边框和交互状态。
- 中文字体优先使用 `Microsoft YaHei UI`，英文数字回退到 `Segoe UI Variable / Segoe UI`。
- 为窗口和文字开启 ClearType、Display Text Formatting、Fixed Hinting、像素对齐和布局取整。
- 重写 Button、TextBox、PasswordBox、ComboBox、ComboBoxItem、DatePicker、Calendar、CheckBox、RadioButton、DataGrid、ToolTip 和 ScrollBar 样式。
- Button 增加 Primary、Secondary、Danger、Icon、Segment 等统一样式。
- 输入控件统一到 44px 高度、8px 圆角和一致的 Normal / Hover / Focus / Disabled 状态。
- DataGrid 统一为 44px 表头、48px 行高、无竖向网格线、轻量交替行、Hover 和 Selected 状态。
- 日期选择器改为 336px Windows 11 风格圆角日历：圆形日期、现代月份导航、当日描边、选中蓝色、淡阴影以及月份/年份视图。
- 重写垂直和横向滚动条，避免默认旧式 WPF 滚动条破坏整体风格。

原因：原项目大量使用 WPF 默认模板，各控件的字体、内边距、圆角和交互状态互不一致，只修改颜色无法解决白角、错位、模糊和老旧感。

启动修复：

- 初版现代日历曾在 `CalendarItem` 模板中写入 `Trigger Property="DisplayMode"`。
- `DisplayMode` 属于外层 `Calendar`，不是 `CalendarItem` 可供普通 Trigger 使用的依赖属性，因此资源字典在启动封装 Style 时抛出“属性对于 Trigger 不能为 null”。
- 当前源码已改为通过 `RelativeSource AncestorType=Calendar` 读取 `Calendar.DisplayMode` 的 `DataTrigger`，保留月份/年份切换效果，同时消除启动崩溃。

### `src/KukaManager/UI/Controls/KukaControls.cs`

改动：

- 增加统一 Button、Text、Icon、Card、Field、ChoiceBox 控件工厂。
- 增加 `RoundedClipBorder`，卡片内容按圆角裁切。
- Button 统一为 42px 高度和统一最小宽度。
- 表单标签与控件统一为 8px 间距，字段之间统一为 20px。
- 所有 ChoiceBox 明确使用 `DisplayMemberPath=Text` 与 `SelectedValuePath=Value`。

原因：避免每个页面各自创建不同尺寸、不同 Margin 的控件，也解决圆角卡片中的表格背景露出直角以及 ComboBox 显示 `FormChoice { ... }` 的问题。

### `src/KukaManager/UI/Components/KukaWindowFrame.cs`

改动：

- 移除默认 Windows 标题栏视觉，使用统一的自定义 Header。
- 集成 Logo、应用名称、当前页面、版本号以及最小化、最大化、关闭按钮。
- 使用 Windows DWM 圆角窗口，并保留窗口拖动、双击最大化和调整大小能力。

原因：消除“旧 Windows 窗口 + 网页后台内容”的割裂感，让主窗口、登录窗口和编辑窗口处于同一视觉体系。

### `src/KukaManager/UI/Layout/ScrollCoordinator.cs`

改动：

- 页面 ScrollViewer 负责纵向滚动。
- DataGrid 保留自己的横向滚动。
- DataGrid 到达顶部或底部后将滚轮继续传递给外层页面。
- 支持 `Shift + 鼠标滚轮` 横向滚动宽表格。

原因：解决嵌套 ScrollViewer 与 DataGrid 互相抢滚轮、页面滚不动以及财务宽表无法横向查看的问题。

### `src/KukaManager/UI/Animations/KukaMotion.cs`

改动：

- 页面切换使用 165ms 淡入和 6px 轻位移动画。

原因：提供接近 Windows 11 / Microsoft 365 的轻量反馈，同时避免复杂动画影响低配置电脑和表格滚动性能。

### `src/KukaManager/Views/MainWindow.cs`

改动：

- 主内容区域统一为 32px 左右、24px 顶部页面边距。
- 页面标题改为清晰的 30px 标题 + 13px 副标题，不再使用网页式大横幅卡片。
- 统一 SectionCard 为 24px 默认内边距、16px 下间距。
- 统一 SectionTitle、SectionNote、工具栏、搜索框和按钮间距。
- 表格实际高度按 44px 表头 + 48px 行高计算。
- 左侧导航改为分组 Fluent 导航，并统一图标、选中指示条与行高。

原因：这是全软件对齐的中心入口。统一页面容器后，学员、报名、排课、班级、教师、财务和设置页不再各自形成不同基线。

### `src/KukaManager/Views/FormWindows.cs`

改动：

- 通用编辑表单统一卡片、标题、字段、长文本和底部操作区。
- 所有 Choice / Reference ComboBox 明确配置显示字段和内部值字段。
- DataGrid 自动生成列的正文 TextBlock 使用 `Margin=16,0,16,0`，与表头 `Padding=16,0` 完全一致。
- 空数据行沿用相同基线，不再出现“暂无数据”贴左边而表头向右缩进。
- 学员历史窗口统一 24px 外边距和现代标题字重。

原因：直接修复截图中课程目录、老师、户头概览等所有表格普遍存在的表头/正文错位。

### `src/KukaManager/Views/MainWindow.Finance.cs`

改动：

- 财务四个视图改为独立的现代分段控件。
- 同步文件说明与操作按钮分成上下两个层级。
- 状态文字恢复到可读字号，不再挤在按钮下方。
- 财务筛选区域使用与全局工具栏相同的内边距和换行逻辑。
- 户头、课程收入、全部流水说明使用统一 SectionNote 间距。

原因：原财务顶部同时堆放切换按钮、小字说明和操作按钮，是截图中最明显的拥挤区域，也会在较窄窗口下发生错位。

### `src/KukaManager/Views/FinanceEditWindow.cs`

改动：

- 编辑窗口统一 24px 外边距、表单字段节奏、金额双列间距和长文本高度。
- 信息卡片、标题字重和按钮区域统一到新的设计系统。

原因：修复标签、下拉框、备注和上下内容不在同一节奏上的问题。

### `src/KukaManager/Views/MainWindow.Pages.cs` 与 `MainWindow.Settings.cs`

改动：

- 工作台指标卡、欢迎卡、周排课说明、续费提醒、课程和教师表格统一间距。
- 设置页卡片统一 24px 内边距。
- 说明文字使用统一 SectionNote，不再直接贴住下方表格或按钮。

原因：用户反馈问题并非单个页面，所以所有业务页共同使用的布局入口都进行了统一。

### `src/KukaManager/app.manifest` 与 `Program.cs`

改动：

- 启用 Per-Monitor V2 DPI Awareness。
- 在创建任何 WPF Visual 前再次请求 Per-Monitor V2 DPI 上下文。

原因：避免 125%、150%、2K、4K 屏幕上的字体模糊、控件半像素和跨显示器缩放异常。

### 测试项目

`tests/KukaManager.SmokeTests`：

- 加载全局主题。
- 实例化 ComboBox、DatePicker、Calendar、DataGrid 并执行模板布局。
- 验证登录、学员、教师、课程、报名、班级、排课、课消、财务账户合并和数据库完整性。

`tests/KukaManager.DataSmokeTests`：

- 验证课程初始化、登录密码哈希、学员/教师读写、SQLite 完整性和外键。

## 4. 未改变的内容

- 未改变正式 SQLite 数据文件位置：`%LOCALAPPDATA%\KukaManager\kuka.sqlite3`。
- 未删除或重命名现有业务表。
- 未改变学员、报名、班级、排课、课消、成长记录、财务导入导出和备份恢复的业务口径。
- UI 改动不要求用户重新建库；旧数据继续读取。

## 5. Windows 构建方法

要求：Windows 10/11 x64，安装 .NET 10 SDK。

在源码根目录执行：

```powershell
dotnet restore src\KukaManager\KukaManager.csproj
dotnet build src\KukaManager\KukaManager.csproj -c Release
dotnet publish src\KukaManager\KukaManager.csproj -c Release -r win-x64 --self-contained true -o dist\KukaManager-1.3.0-Windows-x64
```

也可以使用根目录提供的 `BUILD.cmd`、`START_HERE.cmd` 或 PowerShell 构建脚本。

## 6. 测试方法

数据层测试：

```powershell
dotnet run --project tests\KukaManager.DataSmokeTests\KukaManager.DataSmokeTests.csproj -c Release
```

Windows WPF 主题与业务冒烟测试：

```powershell
dotnet run --project tests\KukaManager.SmokeTests\KukaManager.SmokeTests.csproj -c Release
```

## 7. 本次交付前验证结果

- 主程序 Release 构建：0 错误、0 警告。
- Windows WPF 冒烟测试项目构建：0 错误、0 警告。
- 数据层冒烟测试：5/5 通过。
- 主题 XAML XML 结构检查通过。
- 已确认错误的 `CalendarItem DisplayMode` 普通 Trigger 不再存在。
- self-contained 发布包包含 .NET、WPF、SQLite x64 运行依赖。

## 8. 重要说明

如果后续修改 `Theme.cs` 中的运行时 XAML，必须在 Windows 上运行 `KukaManager.SmokeTests`，不能只看“主程序编译成功”。运行时字符串形式的 ResourceDictionary 不会由普通 C# 编译器完成全部 WPF Style 封装校验；之前的启动异常正是因此在 Windows 启动阶段才暴露。
